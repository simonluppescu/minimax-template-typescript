import Player from "./player";

class Move {
  player: Player;
  //TODO: Add remaining properties of move

  /**
   * TODO: Implement this if there are more properties in the
   * class.
   */
  constructor(player: Player) {
    this.player = player;
  }
}

export default Move;
